[project]
name = "missing-link-langgraph"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "agentstate-reducer[langgraph]>=0.5.0",
    "langchain-core>=1.6.4",
    "langgraph>=1.2.12",
    "langgraph-memory>=0.1.0",
    "langgraph-store-core>=0.1.1",
    "pytest>=9.1.1",
]

[tool.pytest.ini_options]
pythonpath = ["."]
